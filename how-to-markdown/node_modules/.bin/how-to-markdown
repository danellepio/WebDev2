#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../how-to-markdown/bin/how-to-markdown" "$@"
  ret=$?
else 
  node  "$basedir/../how-to-markdown/bin/how-to-markdown" "$@"
  ret=$?
fi
exit $ret
